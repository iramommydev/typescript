// * Base
import { memo } from 'react';
import React from 'react';
// * Components
import CommentsList from '../../../components/CommentsList/CommentsList';

const List = memo(() => <CommentsList />);
export default List;
